export const selectApiNotification = (state) => state.notification;
export const selectUser = (state) => state.user;
export const selectAccessToken = (state) => state.accessToken.access_token;
export const selectBlockedNotification = (state) => state.blockedNotification;
